name: brick_collector
description: Import Rebrickable Parts and collet them
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:

  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  brick_lib:
    path: /Users/wagenblast/develop/brick-lib
  #    git:
  #      url: https://github.com/ObiJanKenobi/brick-lib
  #      ref: 0.0.3

  cupertino_icons: ^1.0.2

  logger: 1.1.0

  json_annotation: ^4.4.0

  get_it: ^7.2.0
  get_it_mixin: ^3.1.4
  flutter_native_splash: ^2.2.17
  dio: 5.7.0
  cached_network_image: ^3.2.3
  flutter_svg: ^2.0.10+1
  go_router: ^14.1.4

  path_provider: ^2.0.11 # get platform path

  flutter_slidable: 4.0.0

  url_launcher: ^6.1.8

  intl: ^0.18.0
  # Local DB
  isar_community: ^3.3.0 # New Version, other project is dead. Needed for 16kb
  isar_community_flutter_libs: ^3.3.0
  rxdart: ^0.27.7
  adaptive_dialog: ^2.1.0
  image_picker: ^1.1.2

  # Firebase
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

  isar_community_generator: ^3.3.0-dev.3

  build_runner: ^2.1.7
  json_serializable: ^6.1.4

  flutter_speed_dial: ^6.2.0
  msix: ^2.7.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
