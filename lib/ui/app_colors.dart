import 'dart:ui';

class AppColors {
  // Backgrounds
  static const bg = Color(0xFF0F0F0F);
  static const surface = Color(0xFF1A1A1A);
  static const surfaceLight = Color(0xFF252525);

  // Accent
  static const highlightColor = Color(0xFFFF6700);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFAAAAAA);

  // Legacy aliases (used across the app)
  static const navItemBgColor = surface;
  static const contentColor = Color(0xFFD0D0D0);
  static const seedColor = Color(0xFFFF6700);
}
